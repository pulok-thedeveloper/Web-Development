const fibo = [0, 1];
for(let i = 2; i <= 25; i++){
    fibo[i]=fibo[i-1] + fibo[i-2];
}

console.log(fibo);

let flor = Math.floor(13.67);
console.log(flor);

const number = -78; 
const answer = Math.abs(number); 
console.log(answer);